// The module only for re-export base module classes (/modules/screensaver/base/*).
//      1. It allows to get rid  "../../../../" import paths.
//      2. It allows to avoid updating nested files when the base class is changed.

export * from '../../base';
